# AI Configuration
GEMINI_API_KEY=

# Firebase Configuration
# Handled automatically via firebase-applet-config.json

# Email Configuration (for daily digests)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Optional: YouTube API Key for video scraping
YOUTUBE_API_KEY=
